ktor:
  deployment:
    port: 8080
  application:
    modules:
      - com.example.DatabaseModuleKt.configureDatabase
      - com.example.RoutingKt.configureRouting

database:
  jdbcUrl: "jdbc:mysql://localhost:3306/prototypes?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
  username: "root"
  password: "password"
  maximumPoolSize: 10
